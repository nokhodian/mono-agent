{
  "credential_platform": null,
  "fields": [
    { "key": "targets", "label": "Profiles", "type": "array", "required": true, "item_type": "text", "help": "Usernames or profile URLs whose followers to export." },
    { "key": "limit",   "label": "Max Results", "type": "number", "required": false, "default": 100 }
  ]
}
